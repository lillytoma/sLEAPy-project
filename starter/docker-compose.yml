services:
  postgres:
    image: postgres:15-alpine
    container_name: sleapy-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: n3u3d4
      POSTGRES_DB: sleapy_db
    ports:
      - "8101:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: